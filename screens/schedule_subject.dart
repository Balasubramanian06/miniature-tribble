import 'package:flutter/material.dart';

class ScheduleSubject extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Schedule'),
    );
  }
}
