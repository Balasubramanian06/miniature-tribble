import 'package:flutter/material.dart';

class AdditionalLearning extends StatelessWidget {
  static const routeName='/additionallearning';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('AdditionalLearning'),
      ),
      body: Center(
        child: Text('Hello'),
      ),
    );
  }
}
